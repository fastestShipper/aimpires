# Add your Anthropic API key here to have citizens produce real work.
# Without this, outputs are simulated placeholders.
ANTHROPIC_API_KEY=

# Optional: override the default Claude model.
# AIMPIRES_MODEL=claude-sonnet-4-6
